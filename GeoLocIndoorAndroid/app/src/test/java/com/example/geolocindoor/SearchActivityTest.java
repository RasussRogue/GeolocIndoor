package com.example.geolocindoor;

import org.junit.Test;
import org.junit.runner.RunWith;
import org.robolectric.RobolectricTestRunner;
import org.robolectric.annotation.Config;

@RunWith(RobolectricTestRunner.class)
@Config(sdk = 28)
public class SearchActivityTest {

    /* @Rule
    public ActivityTestRule<SearchResultsActivity> activityRule = new ActivityTestRule<>(SearchResultsActivity.class); */

    @Test
    public void whenClickOnEventRadioButton_thenPlacedRadioButtonIsUnchecked() throws Exception {

        /*
        /* RadioButton placeRadioButton = (RadioButton) activityRule.getActivity().findViewById(R.id.placeRadioButton);
        RadioButton eventRadioButton = (RadioButton) activityRule.getActivity().findViewById(R.id.eventRadioButton);

        assertEquals(true, placeRadioButton.isChecked());

        eventRadioButton.performClick();

        assertEquals(false, placeRadioButton.isChecked());*/

    }

}
